import SwiftUI

// App entry point. `@main` tells the runtime this is the program's start.
// `App` is the SwiftUI protocol describing the app and its window scenes.
@main
struct OokReaderApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
