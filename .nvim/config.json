{
  "commands": {},
  "commands-old": {
    "dx:bundle": "dx bundle --bundle macos --release",
    "desktop:dx:serve": "dx serve --platform desktop",
    "ios:dx:serve": "dx serve --platform ios",
    "test": "RUST_BACKTRACE=1 cargo test",
    "validate": "cargo test && cargo clippy --all-targets"
  }
}
