import XCTest
@testable import OokReader

// Minimal placeholder test so the test target compiles and `<leader>xt` works.
final class OokReaderTests: XCTestCase {
    func testScaffoldSanity() {
        XCTAssertEqual(2 + 2, 4)
    }
}
